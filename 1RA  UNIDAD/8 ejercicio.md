# DESCRIPCION DEL PROBLEMA
Realiza un programa que permita calcular el promedio de x notas utilizando el ciclo fort y muestre en pantalla si el alumno aprobo o reprobo el grado.

# SOLUCION DEL PROBLEMA 
#include <iostream>
#include <stdlib.h>
#include <stdio.h>

using namespace std;
int main(){ 
float cant_notas, sum,prom,nota,i=0;

prom=sum/cant_nota;
if(prom<=60){ 
    cout<<"Aprovado";
    }
    else { 
        cout<<"Reprobado";
         }
         system ("pause");
          }
          